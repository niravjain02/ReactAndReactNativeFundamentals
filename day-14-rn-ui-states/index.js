import { registerRootComponent } from "expo";

import App from "./App";

// registerRootComponent connects the App component to Expo Go, simulators,
// emulators, and native builds without extra setup.
registerRootComponent(App);
