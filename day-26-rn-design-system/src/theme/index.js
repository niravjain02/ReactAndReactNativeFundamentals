// This single export is the public entry point for the theme. Components do
// not need to know where each token lives, which simplifies theme management.
export { default as colors } from './colors';
export { default as spacing } from './spacing';
export { default as typography } from './typography';
