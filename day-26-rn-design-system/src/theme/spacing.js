// Spacing tokens avoid slightly different padding values across screens.
// As an app scales, changing this scale updates every component that uses it.
const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
  xxl: 32,
  xxxl: 40,
};

export default spacing;
